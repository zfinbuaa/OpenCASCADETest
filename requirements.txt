# OCCT Python bindings (tested with 7.8.1 via conda)
# conda create -n pyoccenv python=3.11 pythonocc-core=7.8.1 -c conda-forge -y
# conda activate pyoccenv
pythonocc-core>=7.8.1

# Mesh processing & validation
numpy>=1.24.0
trimesh>=3.21.0

# Note: pythonocc-core 7.9.3 also works; API may differ slightly
# (e.g., write_gltf_file vs export_shape_to_gltf_file)
